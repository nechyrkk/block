{
  "name": "progress-block",
  "version": "1.0.0",
  "private": true,
  "description": "Progress block prototype for mobile web",
  "scripts": {
    "build": "sass styles/main.scss:styles/main.css --style=compressed --no-source-map",
    "watch": "sass styles/main.scss:styles/main.css --watch"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
